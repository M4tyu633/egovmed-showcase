{
  "name": "egovmed-backend",
  "version": "0.1.0",
  "private": true,
  "description": "eGovMed — client-agnostic backend for the AI-triage + verified-identity + portable-records + payments layer on the eGov API stack (PGH pilot).",
  "main": "api/index.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "node -e \"require('./src/store').seedDemoData().then(()=>{console.log('seeded');process.exit(0)})\"",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.2",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "zod": "^3.23.8"
  }
}
